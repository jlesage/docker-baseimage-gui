--- a/src/winmenu.c
+++ b/src/winmenu.c
@@ -115,7 +115,7 @@
          }
       }

-      CreateWindowLayerMenu(menu, np);
+      //CreateWindowLayerMenu(menu, np);

       if(settings.desktopCount > 1) {
          if(!(np->state.status & STAT_STICKY)) {
